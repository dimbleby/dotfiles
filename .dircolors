# Overrides for the system default LS_COLORS database (see .bashrc, which
# merges this with `dircolors --print-database` so the rest of the database
# always tracks whatever coreutils is installed on the current machine).
DIR 01;36
LINK 01;31
CAPABILITY 30;41
